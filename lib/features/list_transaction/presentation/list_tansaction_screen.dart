import 'package:flutter/material.dart';

class ListTrxScreen extends StatelessWidget {
  const ListTrxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('List Transaction Screen'),
      ),
    );
  }
}
